import 'package:flutter/material.dart';

class AddPlaceForAdmin extends StatelessWidget {
  const AddPlaceForAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add new place'),
      ),
    );
  }
}
