import 'package:flutter/material.dart';

class AddTripPage extends StatelessWidget {
  const AddTripPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add New Trip Page'),
      ),
    );
  }
}
